---
description: Push Changes and Merge branches.
---

<Prompt>

Changes were approved.

1. Merge the `dev` branch with the `staging` and `main` ones.
2. Push and commit the latest changes to the remote `HEAD` of this local GitHub repository.
3. Switch back to the `dev` branch to continue working.

</Prompt>
